<article>
    <h3 id="product<?= $this->product->getId() ?>"><?= $this->product->getName() ?></h3>
    <p>
        <a href="http://alpha-hydro.loc/catalog/<?= $this->product->getFullPath() ?>" target="_blank"><?= $this->product->getFullPath() ?></a>
    </p>
    <h4>Свойства</h4>
    <table class="table table-striped table-condensed product-property">
        <?php foreach ($this->productParams as $param):?>
            <tr>
                <td><?= $param->getName() ?></td>
                <td><?= $param->getValue() ?></td>
            </tr>
        <?php endforeach;?>
    </table>

    <h4>Модификации</h4>
    <table class="table table-striped table-condensed product-property">
        <thead>
            <tr>
                <td>Название</td>
            <?php foreach ($this->modificationsProperty as $property):?>
                <td><?=$property->getName()?></td>
            <?php endforeach;?>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($this->modifications as $modification):?>
            <tr>
                <td colspan="<?= count($this->modificationsProperty)+1 ?>"><?= $modification->getSku() ?></td>
            </tr>
        <?php endforeach;?>
        </tbody>
    </table>
</article>
